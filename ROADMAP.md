# 未来更新计划（Roadmap）

> 最近更新：2026-08-30。Countdown 是纯静态页面，计划以小步迭代为主。

## 短期

- 支持自定义目标日期在界面内直接配置（当前需修改 `js/config.js`）。
- 补充移动端小屏样式检查，验证 320px 宽度下的数字布局。

## 中期

- 增加多目标倒计时（如多个节日/活动并存，在页面内切换）。
- 提供构建极简版（单文件 HTML）的打包脚本，便于离线分发。

## 长期 / 想法

- 评估 PWA 化（离线缓存 + 添加到主屏幕），与现有 localStorage 主题偏好整合。
